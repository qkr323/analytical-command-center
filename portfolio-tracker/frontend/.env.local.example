# Backend URL — use localhost for local dev, Railway URL for production
BACKEND_URL=http://localhost:8000

# Must match API_SECRET in backend .env — never exposed to the browser
API_SECRET=your_api_secret_here
